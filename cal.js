// Basic Calculator

const add = (a, b) => { //더하기
    return a + b;
}

const sub = (a, b) => { //뻬기
    return a - b;
}

const mul = (a, b) => { //곱하기
    return a * b;
}

const div = (a, b) => { //나누기
    return a / b;
}

///Merge Test